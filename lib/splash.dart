import 'package:flutter/material.dart';

class secondclss extends StatelessWidget {
  const secondclss({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}